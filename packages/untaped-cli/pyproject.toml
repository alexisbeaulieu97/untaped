[project]
name = "untaped-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Alexis Beaulieu", email = "alexisbeaulieu@protonmail.com" },
]
requires-python = ">=3.12"
dependencies = [
    "typer",
    "untaped-core",
    "untaped-ansible",
    "rich>=14.1.0",
]

[project.scripts]
untaped = "untaped_cli:app"

[build-system]
requires = ["uv_build>=0.8.16,<0.9.0"]
build-backend = "uv_build"
